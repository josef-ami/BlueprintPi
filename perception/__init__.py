"""Map-based perception: static localization + semantic (pillar/parking) belief.

Built on the existing nav/ stack (geom, localize, pillarmap) and sensors/.
Runnable on the Pi with a real RPLidar, or on any machine in --sim mode.
"""
